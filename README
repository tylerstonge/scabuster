# Scabuster

## Introduction

> A directory brute-forcer written in Scala.

## Code Samples

>`scala scabuster_2.12-1.0.jar +url=razvrat.org +list=wordlist.txt +threads=10 +sockets=10`

## Installation

> Run with `sbt run`, package with `sbt package` the resulting jar can be run with `scala  target/scala-2.12/scabuster_2.12-1.0.jar`
